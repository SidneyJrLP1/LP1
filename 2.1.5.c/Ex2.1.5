#include <stdio.h>
int main(void) {
   int n;
   int soma = 0;
   while (n != 0) {
     printf("Valor: ");
     scanf("%d", &n);
     soma = soma + n;
     }
   printf("Soma de todos os valores: %d\n", soma);
   return 0;
}
